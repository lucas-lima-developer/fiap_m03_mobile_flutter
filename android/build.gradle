buildscript {
    repositories {
        google()  // Repositório do Google (Firebase e outros plugins)
        mavenCentral()
    }
    ext.kotlin_version = '2.1.10'
    dependencies {
        // Use a versão estável mais recente do plugin do Google Services
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
        classpath 'com.google.gms:google-services:4.4.0' // 🔥 Versão recente
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.buildDir = "../build"
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}

subprojects {
    project.evaluationDependsOn(":app")
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
