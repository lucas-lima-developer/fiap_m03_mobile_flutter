name: fiap_m03_mobile_flutter
description: "A new Flutter project."

publish_to: "none" # Indica que o projeto não será publicado no pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0" # Define um intervalo de versões estáveis do Dart

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # 🔥 Firebase - Use versões compatíveis com Flutter 3.x
  firebase_core: ^3.12.0
  firebase_auth: ^5.5.0

  # Gerenciamento de estado com Provider
  provider: ^6.1.2
  firebase_storage: ^12.4.4
  cloud_firestore: ^5.6.4
  fl_chart: ^0.70.2
  intl: ^0.19.0
  infinite_scroll_pagination: ^4.1.0
  brasil_fields: ^1.15.0
  file_picker: ^9.0.2
  open_file: ^3.5.10
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0 # Certifique-se de usar uma versão compatível

flutter:
  uses-material-design: true
